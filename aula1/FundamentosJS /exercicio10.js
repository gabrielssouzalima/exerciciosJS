/* Exercício 10 = Crie três variáveis: produto, preco e quantidade. 
Calcule o valor total da compra e mostre no console */
const produto = "Macarrão";
let preco = 4.32
let quantidade = 4
const calcularValor = (preco * quantidade);
console.log(`Você comprou ${quantidade} ${produto}(s), 
    totalizando ${calcularValor}`);